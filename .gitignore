node_modules
dist
dist-archivo
dev-dist
*.local
.DS_Store
*.tsbuildinfo
.wrangler

# Capacitor / Android (se regenera con `npx cap add android`)
android/
assets/

# La llave para firmar el APK y su contraseña. Nunca a git: quien la tenga puede
# publicar actualizaciones que los teléfonos aceptarán como si fueran tuyas.
*.keystore
*.jks
keystore.properties
# Cualquier copia suelta: al abrir keystore.properties y guardarlo como .txt, Windows
# toma la primera línea como nombre y el archivo se escapa de las reglas de arriba.
*Credenciales*
*.properties.txt
